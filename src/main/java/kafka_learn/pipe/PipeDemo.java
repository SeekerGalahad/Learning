package kafka_learn.pipe;

public class PipeDemo
{
//    public static void main(final String[] args) throws Exception {
//        final Properties props = new Properties();
//        props.put("application.id", "streams-pipe");
//        props.put("bootstrap.servers", "localhost:9092");
//        props.put("key.serde", Serdes.String().getClass());
//        props.put("value.serde", Serdes.String().getClass());
//        props.put("auto.offset.reset", "earliest");
//        final KStreamBuilder builder = new KStreamBuilder();
//        builder.stream(new String[] { "streams-file-input" }).to("streams-pipe-output");
//        final KafkaStreams streams = new KafkaStreams((TopologyBuilder)builder, props);
//        streams.start();
//        Thread.sleep(5000L);
//        streams.close();
//    }
}
